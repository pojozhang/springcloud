server:
  port: 8040

security:
  basic:
    enabled: true
    path: /manager
  user:
    name: admin
    password: admin123
management:
  security:
    enabled: true
  context-path: /manager

zuul:
  prefix: /api
  routes:
    book-service-route:
      service-id: book-service
      path: /book-service/**